name: projeto_autoral
description: "Conecta — plataforma educacional e social para estudantes criadores."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.2.0
  flutter_riverpod: ^3.0.0
  google_fonts: ^6.3.0
  supabase_flutter: ^2.10.0
  cached_network_image: ^3.4.1
  file_picker: ^10.3.0
  image_picker: ^1.2.0
  intl: ^0.20.2
  url_launcher: ^6.3.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
